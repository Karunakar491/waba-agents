{
  "_meta": {
    "version": "1.1.0",
    "lastUpdated": "2026-07-22",
    "description": "Flat codebase index. Query this before reading any file. One entry per file. Update after every approved change.",
    "howToQuery": "Filter by domain or name. Navigate to path. Read only that file.",
    "howToUpdate": "After approved change: update summary and lastUpdated for affected entries. Add new entry for new files. Remove entry for deleted files."
  },
  "index": [
    {
      "path": ".claude/skills/persona-design-evaluator.md",
      "purpose": "Design Evaluator persona — world-class outside eye, runs cold after UX gate on new screens/DESIGN.md/token changes; BLOCKs only on generic AI-generated feel; session model"
    },
    {
      "path": ".claude/skills/persona-el.md",
      "purpose": "Engineering Lead persona — code, debug, PR review, implementation standards"
    },
    {
      "path": ".claude/skills/persona-em.md",
      "purpose": "Engineering Manager persona — architecture, tech decisions, delivery risk"
    },
    {
      "path": ".claude/skills/persona-pm.md",
      "purpose": "Head of Product persona (rewritten 2026-07-22) — 20yr SaaS/ex-Salesforce visionary voice; holds conviction across full discussion, revisits unresolved threads unprompted, does not fold when user answers only the latest objection"
    },
    {
      "path": ".claude/skills/persona-ux.md",
      "purpose": "UI/UX Designer persona gate — whole-app-context review, DESIGN.md S5 checklist step 0, token single-source enforcement, P0 flow UX checklist"
    },
    {
      "path": "AGENT-WORKFLOW.md",
      "purpose": "Maker-checker flow, model assignments, gate batching, escalation — frontend sequence UX -> Design Evaluator -> EL (EL always final)"
    },
    {
      "path": "CLAUDE.md",
      "purpose": "Master constitution — personas, standards, non-negotiables, file pointers"
    },
    {
      "path": "DESIGN.md",
      "purpose": "Design system single source of truth — agent guide preamble, S0 visual theme (5 signature moves + AI-template anti-patterns), S0.1 breakpoints, tokens, typography, spacing, S5 15-item interaction checklist, S6 patterns (incl ConsequenceLine), voice"
    },
    {
      "path": "Meta Business Agent mdh_spices postman_collection 8 July v2.json",
      "purpose": "Postman collection — Meta Business Agent API calls for mdh_spices test business (8 July v2)"
    },
    {
      "path": "TASKS.md",
      "purpose": "Running task list — 22 tasks covering audit fixes, architecture decisions, implementation, deployment"
    },
    {
      "path": "TECH-STACK.md",
      "purpose": "Approved tools and versions — Shadcn/ui + Tailwind replaces Ant Design as of 2026-07-20"
    },
    {
      "path": "backend/lombok.config",
      "purpose": "Lombok config — copyableAnnotations includes @Qualifier so it forwards to generated constructors"
    },
    {
      "path": "backend/pom.xml",
      "purpose": "Maven build — Spring Boot 3.3.2, Java 21, JJWT RS256, Testcontainers MySQL+Redis, RestClient (no WebFlux)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/PlatformApplication.java",
      "purpose": "Spring Boot entry point — validates NODE_ID env var on startup, fails fast if missing"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/exception/BusinessException.java",
      "purpose": "400 exception — thrown for business rule violations"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/exception/GlobalExceptionHandler.java",
      "purpose": "Global exception handler — maps NotFoundException→404, BusinessException→400, validation errors→400"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/exception/NotFoundException.java",
      "purpose": "404 exception — thrown when entity not found"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/id/TsidGenerator.java",
      "purpose": "TSID ID generator — Hibernate IdentifierGenerator. 42-bit timestamp + 10-bit NODE_ID + 12-bit seq. NODE_ID from env var."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/response/ApiResponse.java",
      "purpose": "Generic API response wrapper — ok(data), ok(), error(message)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/security/JwtAuthFilter.java",
      "purpose": "JWT auth filter — reads access_token from HttpOnly cookie. Logs JWT parse failures at DEBUG level (not warn — routine expired tokens would flood logs)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/security/JwtService.java",
      "purpose": "RS256 JWT — reads key pair from file paths (jwt.private-key-path, jwt.public-key-path). Generates and validates access tokens."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/security/SecurityContextHelper.java",
      "purpose": "Static helper to read current TenantDetails (tenantId/userId) from Spring SecurityContext"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/security/SecurityService.java",
      "purpose": "Auth business logic — register, login, refresh, logout. httpOnly cookies. Redis lockout (5 fails → 15min). Refresh token rotation with reuse detection. register() (2026-08-03) also seeds AccountModule.BUSINESS_AGENTS=true for the new account — new signups get the base product by default; module gating is opt-in only for future modules."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/security/TenantDetails.java",
      "purpose": "Authenticated principal record carrying tenantId + userId, set by JWT filter"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/security/ModuleAccessFilter.java",
      "purpose": "Runs after JwtAuthFilter in the security chain (2026-08-03) — 403s any authenticated request when the CALLER'S REQUIRED MODULE (see requiredModule(), path-prefix routed — 2026-08-04, was hardcoded to BUSINESS_AGENTS for the whole /api/v1/** surface until a Template-Studio-only account would have been wrongly blocked) is disabled for the account. Exempts /auth/**, /webhook/**, /actuator/**, and GET /modules/entitlements itself. Fails closed on missing entitlement row (delegates to AccountModuleService). /api/v1/templates/** requires TEMPLATE_STUDIO; everything else defaults to BUSINESS_AGENTS."
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/common/security/ModuleAccessFilterTest.java",
      "purpose": "MockMvc coverage for ModuleAccessFilter — enabled passes, disabled/missing-row both 403, entitlements endpoint exempt even when disabled, unauthenticated stays 401 (not shadowed by the module filter)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/config/RabbitMqConfig.java",
      "purpose": "RabbitMQ config — webhook.raw queue with DLQ, exchange, bindings"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/config/RedisConfig.java",
      "purpose": "Two Redis databases — DB0=cacheRedisTemplate(@Primary), DB1=securityRedisTemplate. Both LettuceConnectionFactory beans."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/config/SecurityConfig.java",
      "purpose": "Spring Security config - CSRF disabled (SameSite=Strict cookie mitigates, not statelessness), CorsConfigurationSource bean (origins from cors.allowed-origins/CORS_ALLOWED_ORIGINS, allowCredentials, trimmed origin list), auth endpoints public, JWT filter + RateLimitFilter wired before UsernamePasswordAuthenticationFilter, BCrypt cost 12; ModuleAccessFilter (2026-08-03) added after JwtAuthFilter — module entitlement gate runs once tenant details are resolved"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/controller/AgentController.java",
      "purpose": "Agent REST endpoints - CRUD incl PUT /{id}, POST /generate-defaults (Claude wizard defaults), deploy/pause, GET /{id}/settings, POST /{id}/thread-control/release, skills/faq/files/websites subresources (2026-07-28: GET-list/GET-single/PUT added for skills/faq/websites, GET-list/single for files — closes the CRUD-completeness gap found via live testing), connectors GET-single/PUT/logs, tools POST /run, phone bind. PUT /{id}/settings/audience (2026-08-04) makes ai_audience operator-settable for the first time (previously read-and-preserved only); GET/POST /{id}/allowlist + DELETE /{id}/allowlist/{entryId} — real use case: QA against test numbers, client-controlled phased rollouts."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/dto/AgentRequest.java",
      "purpose": "Agent create/update request DTO — name, customerFacingName, businessDescription, systemPrompt, tone, language, behaviorRules, handoffEnabled/handoffMessage"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/dto/BindPhoneRequest.java",
      "purpose": "PUT /agents/{id}/phone body — phoneNumberId + internal wabaId"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/dto/FaqRequest.java",
      "purpose": "FAQ create request DTO — question + answer with validation"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/dto/GenerateDefaultsRequest.java",
      "purpose": "Wizard auto-generation request - businessDescription max 200"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/dto/GenerateDefaultsResponse.java",
      "purpose": "Wizard defaults response - generated flag, tone/language/behaviorRules, 5 FAQ pairs; fallback() when Claude unavailable"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/dto/SkillRequest.java",
      "purpose": "Agent skill create request DTO"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/dto/WebsiteRequest.java",
      "purpose": "Agent knowledge website URL request DTO"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/dto/AudienceRequest.java",
      "purpose": "ai_audience update request DTO (2026-08-04) — @Pattern validates EVERYONE|ALLOWLISTED_ONLY, @JsonProperty maps snake_case ai_audience (no global Jackson naming strategy configured in this codebase)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/dto/AllowlistRequest.java",
      "purpose": "Allowlist add-entry request DTO (2026-08-04) — @Pattern enforces E.164 format on consumer_phone_number before it ever reaches Meta (EL round-1 REJECT caught the missing validation)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/entity/Agent.java",
      "purpose": "Agent entity - TSID PK (JSON string via ToStringSerializer), accountId (informational creator only post-2026-07-28 decoupling, NOT access control), updatedBy (last-editor stamp), wabaId (now ToStringSerializer'd too), displayName, customerFacingName, Channel enum (locked after create), tone, language, behaviorRules, systemPrompt, Status, phone binding, handoffEnabled/handoffMessage, sharedAccountCount (transient, populated by AgentAccessService), faqReconciledAt/fileReconciledAt/websiteReconciledAt (TASK-059/062 — one TTL gate per domain so reconciliation doesn't call Meta on every read; shared constant is AgentService.META_RECONCILE_TTL_MINUTES)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/entity/AgentFaq.java",
      "purpose": "AgentFaq entity — TSID PK, agentId, accountId, question, answer, metaFaqId. TASK-059: metaSynced (false = last write failed or reconciliation found this row missing from Meta), metaSyncAttempted (false only for FAQs created while the agent had no phoneNumberId yet — reconciliation must skip these, not flag them as broken)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/entity/AgentFile.java",
      "purpose": "AgentFile entity — TSID PK, agentId, accountId, fileId (Meta file handle), filename, mimeType. TASK-062: metaSynced (false = reconciliation found this file missing from Meta's live list — no metaSyncAttempted needed here unlike AgentFaq, since addFile already throws hard on a failed Meta upload, no local-only save path exists)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/entity/AgentSkill.java",
      "purpose": "AgentSkill entity — TSID PK, agentId, accountId, skill instructions"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/entity/AgentWebsite.java",
      "purpose": "AgentWebsite entity — TSID PK, agentId, accountId, url. Meta crawls natively. TASK-062: metaSynced (false = reconciliation found this website missing from Meta's live list), verified live via a real drift repro (corrupted a local metaWebsiteId to a nonexistent value, confirmed reconciliation flipped it to false, restored)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/entity/AgentWebsitePage.java",
      "purpose": "AgentWebsitePage entity — TSID PK, websiteId, accountId, page URL"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/repository/AgentFaqRepository.java",
      "purpose": "AgentFaq repo - findAllByAgentId, findByIdAndAgentId (fullTextSearch removed with SearchService)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/repository/AgentFileRepository.java",
      "purpose": "JPA repository for AgentFile knowledge uploads"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/repository/AgentRepository.java",
      "purpose": "Agent JPA repository — findByPhoneNumberId, findAllAccessibleByAccount (waba_account_access-joined query, replaces old findByIdAndAccountId/findAllByAccountId post-decoupling — use AgentAccessService, not this repo directly, for access checks)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/repository/AgentSkillRepository.java",
      "purpose": "JPA repository for AgentSkill"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/repository/AgentWebsitePageRepository.java",
      "purpose": "AgentWebsitePage repo - deleteAllByWebsiteId (fullTextSearch removed with SearchService)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/repository/AgentWebsiteRepository.java",
      "purpose": "JPA repository for AgentWebsite knowledge sources"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/service/AgentDefaultsService.java",
      "purpose": "Claude wizard defaults - strict-JSON prompt, fence stripping, try/catch to fallback (timeouts live on ClaudeApiClient)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/service/AgentDeployService.java",
      "purpose": "Deploy/pause via Meta settings PUT — deploy blocked until phone bound; Meta first, DB second; handoff enabled/message sent from real per-agent Agent fields; releaseThreadControl proxies to ThreadControlClient; deleteFromMeta (delete_agent, requires PAUSED, resets agent to draft) and triggerEvent/getEventStatus (agent_event thin proxy) added; connector/tool methods + putSettings all use MetaApiClient.scopedPath() for agent_id query-param scoping (2026-07-28 fix — real Meta 400s confirmed via live Selenium testing once an agent already has a metaAgentId); getSettings (read-through), getConnector/updateConnector/getConnectorLogs (now includes include_stats/summary_only/top_n params, 2026-08-03), runTool (P0 per PM — the actual connector validation mechanism) all added same pass; getTool/updateTool (tool GET-single/PUT) and upsertConnectorApiKey/upsertConnectorCertificate/upsertConnectorOAuth (credential rotation, connectors.md) added 2026-08-03 — previously deferred, no longer deferred; settings PUT is now a real read-modify-write preserving followup/ai_audience instead of hardcoding them (2026-08-03 Wave 1a fix); updateAiAudience (2026-08-04) makes ai_audience operator-settable for the first time — same read-modify-write as putSettings but ABORTS (does not guess from local Agent.status) if the live read fails, since unlike deploy()/pause() there's no caller-supplied rollout truth to fall back on (EL round-1 REJECT caught the original guessing fallback as a silent-rollout-flip risk); getAllowlist/addToAllowlist/removeFromAllowlist (allowlist.md) added same pass — thin proxies matching Meta's actual add-one/list/delete-by-id shape, deliberately not a full-replace abstraction"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/service/AgentService.java",
      "purpose": "Agent domain service - CRUD via AgentAccessService (waba_account_access, not accountId) post-decoupling, updateAgent/bindPhone use per-agentId lock + stamp updatedBy, bindPhone checks waba_account_access instead of Waba.accountId, skills/faq/files/websites (2026-07-28: get/getSingle/update added for skills+faq+websites, get/getSingle for files — all read from local mirror tables, not live Meta proxy, per EM convention; addSkill's Meta field name fixed from 'body' to 'skill'); deleteAgent does full FK-safe cascade delete (pages->websites->faqs->skills->files->messages->conversations->webhook_raw) inside one @Transactional before deleting the agent row. Legacy AgentSkill rows still write through to Meta immediately (untouched by TASK-050) unless explicitly Promoted to the Skill Library — see domain/skill package. getSkills() now calls ensureSkillsBackfilled() first (TASK-054, 2026-07-30) — reactive, best-effort GET-then-local-INSERT for agents imported/reconciled from Meta whose skills were never locally mirrored; never fails the primary read, never writes to Meta. Only wired into the single-agent read path, NOT the TASK-053 aggregate Skills page (stays batched-only). getFaqs() now calls reconcileFaqs() first (TASK-059, 2026-07-30) — reactive, TTL-gated (Agent.faqReconciledAt, 10 min) GET agent_config/faq compared against local AgentFaq.metaFaqId rows, flips metaSynced to surface drift instead of silently trusting a WARN log; skips rows where metaSyncAttempted=false (draft-agent FAQs never pushed yet). KNOWN LIMITATION: Meta's GET agent_config/faq returns a genuine 500 for phone number 1550000250a22 specifically (confirmed bad/synthetic test data, not a platform bug — other real test numbers like 1103393549522539 work fine); reconciliation fails safe either way (never throws, getFaqs still returns 200). getFiles()/getWebsites() now call reconcileFiles()/reconcileWebsites() first (TASK-062, 2026-07-30) — same shape, TTL-gated via Agent.fileReconciledAt/websiteReconciledAt, but simpler than FAQ's version: no metaSyncAttempted concept needed since addFile/addWebsite already throw hard on a failed Meta write (no local-only save path exists). Drift-detection verified live end-to-end for websites (corrupted a local metaWebsiteId, confirmed the flag flipped, restored)."
    },
    {
      "path": "backend/src/main/resources/db/migration/V20__create_skill_library.sql",
      "purpose": "TASK-050 — creates `skill` (nullable waba_id, mirrors Agent.wabaId) and `agent_skill_attachment` (agent_id, skill_id, meta_skill_id per-attachment since Meta scopes skill ids per phone number, deployed_at) tables for the Skill Library first slice. Supersedes TASK-046's join-table design."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/skill/entity/Skill.java",
      "purpose": "Shared Skill Library row. Editing this NEVER touches Meta — every AgentSkillAttachment compares its deployedAt against this row's updatedAt to compute out-of-sync. waba_id nullable (null = not yet shared)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/skill/entity/AgentSkillAttachment.java",
      "purpose": "Join between one agent and one Library Skill. metaSkillId lives here (not on Skill) since the same Library skill gets a different Meta-side skill id per agent it's attached to. deployedAt null/stale vs skill.updatedAt = 'Out of sync'."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/skill/service/SkillLibraryService.java",
      "purpose": "Library CRUD (createSkill/updateSkill/deleteSkill/listSkills — DB only, never calls Meta), getAgentSkillsView (union of legacy AgentSkill rows + Library-attached rows with computed LIVE/OUT_OF_SYNC status), promote (converts one legacy AgentSkill into a Skill+attachment, deployedAt=now since content is already live at that instant), syncSkills/syncOne (the ONLY path that pushes a Library skill to Meta — deliberately not one @Transactional wrapping the loop, so one item failing doesn't roll back others that already succeeded)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/skill/controller/SkillLibraryController.java",
      "purpose": "POST/GET /api/v1/skills (Library CRUD, wabaId-scoped), PUT/DELETE /api/v1/skills/{id}, GET /api/v1/agents/{id}/skills-view (unified agent+library skill list), POST /api/v1/agents/{id}/skills/{id}/promote, POST /api/v1/agents/{id}/skills/sync."
    },
    {
      "path": "backend/src/main/resources/db/migration/V21__widen_agent_skill_attachment_meta_id.sql",
      "purpose": "Widens agent_skill_attachment.meta_skill_id to VARCHAR(255) — V20 reintroduced the exact truncation bug V14 already fixed for the same class of Meta opaque-ID column; caught live on the first real promote() call, not by EL review."
    },
    {
      "path": "backend/src/main/resources/db/migration/V22__create_skill_template.sql",
      "purpose": "TASK-052 — skill_template table (GLOBAL, no waba_id/account_id — every account sees every row). Seeds 10 genericized starter templates (brand-stripped from this session's real MDH skills), tagged industry/use_case."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/skill/entity/SkillTemplate.java",
      "purpose": "Karix-curated reference catalog entry — global, no tenant scoping at all (intentional: pure reference content). See SkillLibraryService.copyTemplate for the one-way copy-to-Skill mechanic."
    },
    {
      "path": "frontend/src/pages/SkillLibraryPage.tsx",
      "purpose": "TASK-051/052/053 — standalone Skills page at /library/skills (renamed from 'Skill Library' — that name now belongs to the separate reference catalog below). TRUE aggregate (TASK-053): every skill on every agent on the account's first accessible WABA, not just Library rows — legacy AgentSkill rows (source='AGENT', always deployed, grouped collapsed-by-default per agent since they're not deduplicated across agents) merged with Library skills (source='LIBRARY', Deployed/Drafts split). Edit/delete branch by source to the correct endpoint set (legacy /agents/{id}/skills/{id} vs Library /skills/{id}) — do not conflate these, they're different tables with different write paths. 'Browse Skill Library' button links to /library/skills/browse. Multi-WABA switcher explicitly deferred — shows first WABA only, name in page header."
    },
    {
      "path": "frontend/src/pages/SkillTemplateBrowsePage.tsx",
      "purpose": "TASK-052 — Karix-curated reference catalog at /library/skills/browse. Lists skill_template rows (global, same for every account), filterable by industry/use case chips. 'Copy to my Skills' creates a new Draft Skill for the account's WABA (one-way INSERT, no link back to the template)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/service/WebsiteCrawlService.java",
      "purpose": "Crawls agent knowledge websites, stores page content (AgentWebsitePage) for wizard knowledge step"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/analytics/controller/AnalyticsController.java",
      "purpose": "REST endpoints for dashboard analytics — message volume, sessions, agent performance"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/analytics/entity/AgentPerformanceHourly.java",
      "purpose": "Hourly rollup entity for agent message/session metrics (MySQL, ClickHouse dropped 2026-07-19)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/analytics/entity/ConversationSession.java",
      "purpose": "Session window entity for conversation analytics"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/analytics/entity/WebhookEvent.java",
      "purpose": "Normalized webhook event entity for analytics pipeline"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/analytics/repository/AgentPerformanceHourlyRepository.java",
      "purpose": "JPA repository for AgentPerformanceHourly rollups"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/analytics/repository/ConversationSessionRepository.java",
      "purpose": "JPA repository for ConversationSession"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/analytics/repository/WebhookEventRepository.java",
      "purpose": "JPA repository for WebhookEvent"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/analytics/service/AnalyticsService.java",
      "purpose": "Analytics ingest listener, hourly rollup, summary/hourly queries. Access check: verifyAgentOwnership (now via AgentAccessService, waba_account_access-based post-decoupling) runs BEFORE any query, fails closed with NotFoundException"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/controller/ConversationController.java",
      "purpose": "REST endpoints for inbox — list conversations, messages per conversation (tenant-scoped); GET /conversations/counts returns agentId->count map for the Agents list table"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/entity/Conversation.java",
      "purpose": "Conversation entity — tenant/agent/customer thread with status + last message metadata; needsHuman flag (scaffold, provisional) set by HandoffClassifier NEEDS_HUMAN signal"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/entity/Message.java",
      "purpose": "Message entity — direction, type (text/media), status, external Meta message id (idempotency)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/model/InboundMessage.java",
      "purpose": "Record: customerPhone, metaMessageId, textBody, messageType, contentJson — output of InboundMessageParser"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/model/StatusUpdate.java",
      "purpose": "Record: metaMessageId, status, recipientPhone — output of StatusUpdateParser"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/model/HandoffSignal.java",
      "purpose": "Enum: BIZAI_ACTIVE/NEEDS_HUMAN/STATUS_UPDATE/UNRECOGNIZED — output of HandoffClassifier (provisional, see webhook-standby-handoff.md)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/repository/ConversationRepository.java",
      "purpose": "Conversation JPA repository — countByAgentIdForAccount groups by agentId in one query (AgentConversationCount projection) for the Agents list conversation-count column"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/repository/MessageRepository.java",
      "purpose": "Message JPA repository — findByMetaMessageId for status update routing"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/service/ConversationService.java",
      "purpose": "Thin coordinator — parse→persist→analytics. Routes: inbound messages→ConversationStore.saveInbound; status webhooks (delivered/read/failed)→updateMessageStatus; sent status→saveOutbound (Decision D1). markProcessing/markProcessed/markFailed are package-private @Transactional. Calls HandoffClassifier per payload: NEEDS_HUMAN→conversationStore.markNeedsHuman + stubbed notifyHandoverTarget; BIZAI_ACTIVE→log only. getConversationCountsByAgent() backs GET /conversations/counts."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/service/ConversationStore.java",
      "purpose": "All DB reads/writes for conversations and messages — findOrCreate, saveInbound (contentType+contentJson, lastMessageAt in same tx), saveOutbound (nullable content), updateMessageStatus, markNeedsHuman (scaffold, provisional)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/service/InboundMessageParser.java",
      "purpose": "Parses raw Meta webhook JSON → Optional<InboundMessage>. All types persisted. Uses .at() safe path. text→textBody; image/audio/video/document/template/interactive→contentJson."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/service/MetaMessageSender.java",
      "purpose": "Sends reply to Meta API. Returns confirmed message ID. Throws BusinessException if Meta returns no ID — no UUID fallback."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/service/StatusUpdateParser.java",
      "purpose": "Parses Meta status webhook (value.statuses[0]) → Optional<StatusUpdate>. Uses .at() for safe nested path access."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/conversation/service/HandoffClassifier.java",
      "purpose": "Pure classifier — raw payload → HandoffSignal via standby-wrapper structural check (not a field name). PROVISIONAL: based on one sample payload capture, unverified against production traffic. Called from ConversationService; logs INFO on NEEDS_HUMAN and BIZAI_ACTIVE branches."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/controller/AuthController.java",
      "purpose": "Auth endpoints — POST /register, /login, /refresh, /logout. Uses HttpServletResponse for cookie management."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/dto/AuthResponse.java",
      "purpose": "Auth response DTO — userId, accountId, email, role. No tokens (tokens in httpOnly cookies)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/dto/LoginRequest.java",
      "purpose": "Login request DTO — email, password"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/dto/RefreshRequest.java",
      "purpose": "Refresh-token request DTO for /auth/refresh"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/dto/RegisterRequest.java",
      "purpose": "Register request DTO — email, password, companyName"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/entity/BusinessAccount.java",
      "purpose": "BusinessAccount entity — TSID PK, company name, email, passwordHash"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/entity/RefreshTokenFamily.java",
      "purpose": "RefreshTokenFamily entity — familyId (UUID), userId, accountId, tokenHash (SHA-256), revoked flag, revokedReason, expiresAt"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/entity/User.java",
      "purpose": "User entity — TSID PK, accountId FK, email, passwordHash, Role(owner/admin/agent), Status(active/inactive/suspended)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/entity/AccountModule.java",
      "purpose": "Per-account module entitlement (2026-08-03) — account_modules(account_id, module, enabled). Module enum: BUSINESS_AGENTS, TEMPLATE_STUDIO (added 2026-08-04, no account seeded true yet — defaults disabled for everyone until explicitly granted). Admin/DB-set only, no self-serve toggle UI."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/repository/AccountModuleRepository.java",
      "purpose": "AccountModule repo — findByAccountIdAndModule (fail-closed lookup), findAllByAccountId"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/service/AccountModuleService.java",
      "purpose": "isEnabled() fail-closed (missing row = disabled); getEntitlements() returns every known Module defaulted false then overlaid with real rows — single source of truth consumed by both ModuleAccessFilter (backend enforcement) and GET /api/v1/modules/entitlements (frontend nav/route gating)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/controller/AccountModuleController.java",
      "purpose": "GET /api/v1/modules/entitlements — current account's module map, exempted from ModuleAccessFilter itself so a locked-out account can still see why"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/user/service/AccountModuleServiceTest.java",
      "purpose": "Covers fail-closed default (no row/disabled row = false), enabled row = true, getEntitlements default map"
    },
    {
      "path": "backend/src/main/resources/db/migration/V32__create_account_modules.sql",
      "purpose": "account_modules table (BIGINT UNSIGNED per V5 convention) + one-time backfill seeding BUSINESS_AGENTS=true for every pre-existing business_account row — avoids locking out current users when the gate goes live. Backfill row IDs are a plain ROW_NUMBER() sequence, not TsidGenerator (Flyway has no NODE_ID context) — safe because they're far below any real TSID's magnitude and this is a one-time seed of a brand-new table."
    },
    {
      "path": "backend/src/main/resources/db/migration/V33__create_waba_karix_credential.sql",
      "purpose": "HISTORICAL — created waba_karix_credential (per-WABA esme_addr + api_key), DROPPED by V34 same day (2026-08-04) once the real Karix model turned out to be per-esme_addr not per-WABA (confirmed 0 rows in production before the drop). Never modify an applied migration — V34 undoes this one, matching the V29/V30 precedent. V31 (Wave 2, still uncommitted) occupies the V31 slot on disk, hence V33 not V32-follow-on confusion — verify next available number against disk, not just git, before adding a migration."
    },
    {
      "path": "backend/src/main/resources/db/migration/V34__karix_credential_per_phone.sql",
      "purpose": "Corrects the Karix credential model (2026-08-04): drops waba_karix_credential (verified 0 rows first), creates karix_esme_credential (esme_addr UNIQUE, no waba_id — an esme_addr can serve phone numbers under DIFFERENT WABAs, confirmed by founder with real production data) and phone_esme_mapping (waba_id + phone_number_id UNIQUE, FK to karix_esme_credential; phone_number_id is NOT an FK to phone_number_snapshot since that's a resyncable cache and this mapping must survive a resync)."
    },
    {
      "path": "backend/src/main/resources/db/migration/V35__iris_ai_credentials_and_sessions.sql",
      "purpose": "Iris backend (2026-08-04, real build): ai_provider_credential (BYOK, account_id + provider UNIQUE, closed-set provider/model validated server-side via AiProvider enum), iris_session (account_id, nullable waba_id, pending_tool_name/pending_tool_args_json holding a mutating tool call's exact args awaiting user confirmation), iris_message (session_id FK, role USER/ASSISTANT/TOOL, content, optional tool_name/tool_args_json)."
    },
    {
      "path": "backend/src/main/resources/db/migration/V36__add_title_to_iris_session.sql",
      "purpose": "Adds iris_session.title (VARCHAR 120, nullable) for the Claude-style chat-history sidebar (2026-08-04) — set once from a truncated first user message, 'New chat' is a frontend fallback for null, never stored server-side."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/repository/BusinessAccountRepository.java",
      "purpose": "BusinessAccount JPA repository — findByEmail (2026-08-03, added for module-entitlement tests)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/repository/RefreshTokenFamilyRepository.java",
      "purpose": "RefreshTokenFamily JPA repository — findById(familyId)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/user/repository/UserRepository.java",
      "purpose": "User JPA repository — findByEmail, findByIdAndAccountId (used by ClientService to validate staff-grant targets share the caller's account)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/controller/WabaController.java",
      "purpose": "POST /waba/validate, POST /waba, GET /waba. create() fires PhoneNumberSyncService.syncForAccount() (TASK-056) after wabaService.create() returns — lives HERE not in WabaService itself, because PhoneNumberSyncService depends on WabaService (circular-bean-dependency avoidance; caught a real Spring startup failure when first tried as a @Lazy field injection into WabaService — that doesn't work with Lombok constructor injection)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/dto/WabaDtos.java",
      "purpose": "WABA DTOs — validate/create requests, phone list, WabaResponse (id serialized as String for JS safety). TASK-061: PhoneNumber and AccountPhoneNumber both gained qualityRating/nameStatus/messagingLimitTier — previously never requested from Meta at all."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/entity/Waba.java",
      "purpose": "WABA registry entity — account-scoped, Meta WABA ID, label, status"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/entity/KarixEsmeCredential.java",
      "purpose": "A Karix esme_addr identity (one api_key each), added 2026-08-04 replacing WabaKarixCredential (deleted same day — was wrongly modeled as one-credential-per-WABA). Carries account_id for tenant scoping but NO waba_id — the same esme_addr can serve phone numbers under different WABAs (confirmed by founder with real production data). Which WABA(s) it's used for is entirely a property of PhoneEsmeMapping rows. apiKey encrypted via SecretEncryptor, decrypted only at outbound-call time (TemplateStudioService.resolveCredential) — no GET path ever returns it."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/entity/PhoneEsmeMapping.java",
      "purpose": "Maps one phone number (under one WABA) to the KarixEsmeCredential used for it, added 2026-08-04. Many phones can point to the same credential — confirmed by founder with real Karix data (one esme_addr, six phone numbers). phoneNumberId matches PhoneNumberSnapshot.phoneNumberId by value, NOT an FK (snapshot is a resyncable cache; mapping must survive a resync). UNIQUE(waba_id, phone_number_id) enforces one credential per phone."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/repository/KarixEsmeCredentialRepository.java",
      "purpose": "findByEsmeAddr (uniqueness check), findAllByAccountId (dropdown of an account's esme_addr credentials — account-wide, not WABA-scoped, since esme_addr isn't WABA-scoped either)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/repository/PhoneEsmeMappingRepository.java",
      "purpose": "findAllByWabaId (list connected phones), findFirstByWabaId (TemplateStudioService.resolveCredential's pragmatic \"any mapped phone's credential works for a WABA-level template call\" pick), findByWabaIdAndPhoneNumberId."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/infrastructure/crypto/SecretEncryptor.java",
      "purpose": "AES-256-GCM encrypt/decrypt for secrets-at-rest (2026-08-04) — first (and only) consumer KarixEsmeCredential.encryptedApiKey (was WabaKarixCredential.encryptedApiKey before the 2026-08-04 credential-model correction — same encryption utility, just a different entity holding the ciphertext now). Single master key from crypto.master-key/CRYPTO_MASTER_KEY env var, random 12-byte IV per value, no KMS/rotation yet (EM-scoped v1, upgrade path documented in-file, not built until a second consumer or real rotation need exists — this codebase had NO encryption-at-rest utility before this; a prior BYOK-credentials-table proposal was blocked specifically because this didn't exist)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/templatestudio/TemplateStudioClient.java",
      "purpose": "Single point of contact for karix-mcp's REST API (2026-08-04, extended 2026-08-04 with listTemplates/getTemplate/editTemplate/uploadMedia, extended again same day with getAuditLog) — mints a fresh JWT per call via karix-mcp's own POST /oauth/token (per-WABA esme_addr/api_key, confirmed karix-mcp is ALREADY correctly multi-tenant this way, no karix-mcp change needed). Explicit 5s connect/15s read timeout, no retry, no circuit breaker (not in TECH-STACK.md, not justified without repeated-incident evidence). editTemplate/uploadMedia wrap karix-mcp endpoints that themselves wrap karix_client.py's edit_template (REPLACES components entirely, no partial patch) and upload_media (category is 'image'|'video'|'document', not MIME type — a known Karix bug malforms the returned handle's type marker for images, surfaced to the user rather than hidden). getAuditLog calls karix-mcp's GET /api/audit-log — surfaces the api_call_log table karix_client.py already writes to on every call, no new logging added, request/response bodies are pre-redacted by api_call_logger.py before they ever reach that table (secrets never stored, safe to render raw in the UI)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/templatestudio/TemplateStudioException.java",
      "purpose": "karix-mcp call failure — carries HTTP status (isCredentialFailure() for 401/403), mirrors MetaApiException's shape. Raw karix-mcp error body captured for logging only, never surfaced verbatim to the frontend."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/templatestudio/TemplateStudioService.java",
      "purpose": "Proxies Template Studio ops to karix-mcp — the ONLY class calling TemplateStudioClient. Tenant isolation: existsByWabaIdAndAccountId before touching Karix credentials (same fail-closed pattern as AgentAccessService/WabaService). 2026-08-04: added listTemplates/getTemplate/editTemplate/uploadMedia, then getAuditLog, all through the same resolveCredential() gate as create/delete/bulk-import. resolveCredential() rewritten same day for the corrected credential model — picks the FIRST PhoneEsmeMapping found for the WABA (Meta approves templates at the WABA level, not per phone, so any mapped phone's esme_addr is equally valid; pragmatic default per founder's \"try it and test\", not a designed selection rule — revisit if real Karix behavior ever shows the choice matters). Fails closed with a clear message if zero phones are mapped. resolveCredential() made PUBLIC same day (was private) so domain/templatestudio/iris's KarixMessagingClient can reuse the exact same resolution for Iris's send_test_template — never a second, duplicated implementation of this lookup."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/templatestudio/KarixCredentialService.java",
      "purpose": "Replaces WabaKarixCredentialService (deleted 2026-08-04, wrong per-WABA model). listMappings/listUnmappedPhoneNumbers/listEsmeOptions/mapToExistingEsme/mapToNewEsme — every method tenant-checks via existsByWabaIdAndAccountId; mapToExistingEsme ADDITIONALLY verifies the reused esme_addr credential belongs to the caller's own accountId (EL-verified, real cross-tenant leak risk otherwise). mapToNewEsme catches a duplicate-esme_addr DataIntegrityViolationException and returns a BusinessException telling the operator to use the dropdown instead, same for duplicate phone-mapping. Never returns a raw or encrypted api_key — MappingView/EsmeOption carry no such field."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/templatestudio/TemplateRequest.java",
      "purpose": "Create-template request DTO — template_name/language/category/components, @NotBlank/@NotEmpty validated at the boundary"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/templatestudio/EditTemplateRequest.java",
      "purpose": "Edit-template request DTO (2026-08-04) — components/altTempBody/editAltBody/allowCategoryChange. Components REPLACES the template's full component set on Karix's side, not a patch — the frontend must submit the complete desired shape, not a diff."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/templatestudio/MapExistingEsmeRequest.java",
      "purpose": "phoneNumberId + esmeCredentialId request DTO (2026-08-04) for KarixCredentialService.mapToExistingEsme() — reuses an already-configured esme_addr credential for a new phone number."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/templatestudio/MapNewEsmeRequest.java",
      "purpose": "phoneNumberId/esmeAddr/label/apiKey request DTO (2026-08-04) for KarixCredentialService.mapToNewEsme() — creates a brand-new esme_addr credential and maps a phone number to it in one call."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/templatestudio/TemplateStudioController.java",
      "purpose": "REST surface at /api/v1/templates/** (2026-08-04, extended 2026-08-04 with list/get/edit/media, then audit-log, then the corrected credential model) — this exact prefix is what ModuleAccessFilter routes to TEMPLATE_STUDIO, do not move without updating that filter. create/delete/list/get/edit template, media upload, bulk-import + status poll, GET /{wabaId}/audit-log (literal segment, correctly ranked above the /{wabaId}/{templateId} pattern by Spring's path matching, no collision). Old karix-credential get/upsert REPLACED same day by: GET /{wabaId}/phone-mappings, GET /{wabaId}/unmapped-phones, GET /esme-options (account-wide, not WABA-scoped — also a literal segment correctly ranked above /{wabaId}), POST /{wabaId}/phone-mappings/existing-esme, POST /{wabaId}/phone-mappings/new-esme."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/repository/WabaRepository.java",
      "purpose": "Waba repo — account-scoped finders + findByAccountIdAndWabaId; findFirstByWabaIdOrderByIdAsc is the canonical cross-account lookup used post-2026-07-28 decoupling"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/service/WabaService.java",
      "purpose": "Validate WABA against Meta (spec 5.4 error messages); create()/list() now dedup to one canonical Waba row per external ID across accounts + grant waba_account_access (2026-07-28 decoupling — known simplification: per-account label nicknames not supported, shared row); getPhones() triggers WabaAgentReconciliationService. listForAccount/listAllPhonesForAccountId (TASK-055): explicit-accountId variants of list()/listAllPhonesForAccount() for use by the @Async login-sync job (no request-scoped SecurityContext on a background thread). getCachedOrLivePhonesForAccount(): cache-first read of phone_number_snapshot, falls back to the original live per-WABA Meta call only if the cache is empty — that live path is kept, not removed. TASK-061: fetchPhonesFromMeta now requests fields=...,quality_rating,name_status,messaging_limit_tier explicitly (was previously omitted entirely, so Meta only returned its default field set) — extractQualityRating() defensively handles both a flat string and a nested {quality_score:{score:...}} shape since neither is documented in docs/meta-api/."
    },
    {
      "path": "backend/src/main/resources/db/migration/V23__create_phone_number_snapshot.sql",
      "purpose": "TASK-055 — phone_number_snapshot table, scoped per (account_id, phone_number_id) not per WABA (agent visibility on a phone number is account-specific, PhoneNumberAccessGuard). Populated by the login-triggered PhoneNumberSyncService. quality_rating/name_status/messaging_limit_tier added by V26 (TASK-061)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/repository/PhoneNumberSnapshotRepository.java",
      "purpose": "TASK-056 fix #3/#4 — upsert() is a native INSERT..ON DUPLICATE KEY UPDATE on uq_account_phone, atomic at the MySQL row level so two overlapping PhoneNumberSyncService calls can never hit a duplicate-key crash (concurrent inserts of genuinely new rows can still legitimately deadlock at the InnoDB level — handled by a retry loop in the caller, not here). TASK-061: upsert() gained quality_rating/name_status/messaging_limit_tier params/columns — verified the native SQL's column-list/VALUES order matches positionally, not just visually (MySQL native inserts are positional, not by entity field order)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/service/PhoneNumberSyncService.java",
      "purpose": "TASK-055/056 — @Async, own class (Spring proxy pattern already established by WebsiteCrawlService/EvalRollupWorker). syncForAccount(accountId): fetches live via WabaService.listAllPhonesForAccountId, upserts each phone via PhoneNumberSnapshotRepository.upsert (atomic native INSERT..ON DUPLICATE KEY UPDATE), retries up to 3x on ConcurrencyFailureException (InnoDB deadlock under concurrent inserts) via a manually-driven REQUIRES_NEW TransactionTemplate — NOT @Transactional on the method itself, since a self-looping retry needs a genuinely fresh transaction per attempt. Stale-row cleanup (deletes numbers Meta no longer returned) only runs when the fetch was FULLY successful — skipped on any partial WABA failure, so a transient one-WABA blip never wipes that WABA's cached rows. Fire-and-forget from SecurityService.login() and WabaController.create() — never blocks, never throws. Five fix iterations, all verified live under real concurrent load and real partial-failure conditions (see TASKS.md TASK-056)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/security/SecurityService.java",
      "purpose": "Login/register/refresh/logout, lockout tracking (Redis), JWT + opaque refresh token issuance. login() fires PhoneNumberSyncService.syncForAccount() as the last step (TASK-055, @Async, fire-and-forget) — first Meta-sync trigger point in the login flow; more domains (Skills/FAQs) may attach here in future slices."
    },
    {
      "path": "frontend/src/components/shared/ConsequenceLine.tsx",
      "purpose": "Shared one-line consequence/reassurance statement, extracted after 3rd use (Business Profile deploy, Delete-from-Meta, Agent Event)"
    },
    {
      "path": "frontend/src/hooks/useJobPoll.ts",
      "purpose": "Shared bounded status-polling hook for async Meta jobs (Agent Event, Agent Eval run) — terminal states + 'unknown' fallback on poll failure/timeout"
    },
    {
      "path": "frontend/src/components/agent-detail/BusinessProfileTab.tsx",
      "purpose": "AgentDetailPage tab — Business Profile live/history UI for THIS agent's phone number. TASK-058: ProfileEditor, toFormValues, extractMessage, EMPTY_FORM now exported so BusinessPersonaLibraryPage.tsx can reuse the same form/helpers without duplicating them."
    },
    {
      "path": "frontend/src/pages/BusinessPersonaLibraryPage.tsx",
      "purpose": "TASK-058/060 — standalone account-wide Business Persona page, reached via AppShell's 4th AGENT_SUB_NAV entry (/library/persona). TASK-060 redesigned this from a single-phone-number card view into a search/filter/table layout (Name/Status/Phone number/Last touched — no 'Agent' column, since BusinessProfile.phoneNumberId is 1:1, not multi-attached like Skills). Now just query/mutation wiring (152 lines) — composes usePersonaData.ts (data-fetching hook), PersonaTable.tsx, PersonaFilters.tsx (see those entries)."
    },
    {
      "path": "frontend/src/components/persona/usePersonaData.ts",
      "purpose": "TASK-060 — account-wide Business Persona rows: drafts (GET /business-profiles/drafts) fanned out with per-phone-number live+history, client-aggregated into one row-per-profile list. GET /waba/{wabaId}/phones takes the EXTERNAL Meta wabaId (waba.wabaId), NOT the internal DB id (waba.id) — caught live (400 'WABA not found' on every call) after both EL review rounds missed it, since neither checked endpoint-id semantics."
    },
    {
      "path": "frontend/src/components/persona/PersonaTable.tsx",
      "purpose": "TASK-060 — the Persona table + PersonaTableRow sub-component (extracted after an EL REJECT on the original 349-line single-file page). Per-row 'Deploy to...' dropdown state is a Record<draftId, phoneNumberId> keyed map, not a shared variable — verified both times (correctness pass, then again after the component split)."
    },
    {
      "path": "frontend/src/components/persona/PersonaFilters.tsx",
      "purpose": "TASK-060 — PersonaFilters (search + status dropdown) and PersonaDraftEditor (wraps BusinessProfileTab.tsx's shared ProfileEditor), both extracted from the original oversized page during the EL-mandated split."
    },
    {
      "path": "frontend/src/components/agent-detail/DeleteFromMetaModal.tsx",
      "purpose": "Highest-stakes modal — typed phone-number confirmation before DELETE /agents/{id}/meta-agent; visually distinct from the DB-only delete modal"
    },
    {
      "path": "frontend/src/components/agent-detail/TriggerEventModal.tsx",
      "purpose": "Agent Event trigger modal — event type/description/customer phone, status via useJobPoll"
    },
    {
      "path": "frontend/src/components/agent-detail/EvalTab.tsx",
      "purpose": "AgentDetailPage tab — eval case list (paginated), run eval via useJobPoll, plain-language summary"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/reports/service/ReportsService.java",
      "purpose": "Thin proxy to Meta agent-eval (cases/run/details/summary), per-agent; plus account-wide getConversationsReport (success=closed+!needsHuman, no new column) and getApiCallLog read; eval query-string ids URL-encoded before concatenation (2026-08-03 — ids can contain &, #, =)"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/reports/service/ReportsServiceTest.java",
      "purpose": "Covers eval id URL-encoding fix (2026-08-03) for run/pollEvalRun/getEvalDetails/getEvalSummary"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/reports/controller/ReportsController.java",
      "purpose": "REST surface for per-agent agent-eval under /api/v1/reports/agents/{agentId}/eval"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/reports/controller/AccountReportsController.java",
      "purpose": "Account-wide Reports endpoints under /api/v1/reports: conversations, api-calls, eval-rollup (start/poll)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/reports/service/EvalRollupService.java",
      "purpose": "In-memory job store (no persistence) for account-wide eval rollup; start()/poll() only, delegates fan-out to EvalRollupWorker"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/reports/service/EvalRollupWorker.java",
      "purpose": "Separate @Async bean (proxy requirement) — fans out per-agent eval run+poll in parallel with per-agent timeout/try-catch so one dead number doesn't fail the whole rollup"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/infrastructure/meta/audit/ApiCallLog.java",
      "purpose": "One row per Meta API call (method/path/status/timing/redacted body) for account-scoped debugging"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/infrastructure/meta/audit/ApiCallLogRepository.java",
      "purpose": "Repo for ApiCallLog, ordered by called_at desc per account"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/infrastructure/meta/audit/ApiCallLogRedactor.java",
      "purpose": "Allowlist-style secret redaction (key-name regex + connector auth-header {field_name,value} shape) before persisting request/response bodies — never blanket-hides bodies; regex extended 2026-08-03 to catch client_key/private_key (mTLS cert rotation payload) without blanket-redacting public client_certificate/ca_certificate"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/infrastructure/meta/audit/ApiCallLogRedactorTest.java",
      "purpose": "Covers 2026-08-03 client_key/private_key redaction fix — confirms public certificate fields still pass through unredacted"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/infrastructure/meta/audit/ApiCallLogWriter.java",
      "purpose": "Separate @Async bean (proxy requirement) writing ApiCallLog rows; never throws back to MetaApiClient — a failed audit write must not break the real Meta call"
    },
    {
      "path": "backend/src/main/resources/db/migration/V17__create_api_call_log.sql",
      "purpose": "api_call_log table — nullable account_id, method/path/status/timing/redacted bodies"
    },
    {
      "path": "frontend/src/pages/ReportsPage.tsx",
      "purpose": "New Reports nav section — Conversations (volume+success rate+by-channel), Eval (async rollup via useJobPoll, links out to per-agent EvalTab), API Calls (expandable request/response log)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/service/PhoneNumberAccessGuard.java",
      "purpose": "Entity-agnostic tenant-boundary check for anything keyed by phoneNumberId — access = waba_account_access grant on the Agent's WABA (bound), or original creator (unbound draft). Rewrote 2026-07-28 for the Agent/WABA ownership decoupling; hasAccess() non-throwing variant reused by WabaService"
    },
    {
      "path": "frontend/src/components/agent-detail/SkillsTab.tsx",
      "purpose": "Skills tab — reads unified GET /agents/{id}/skills-view (union of legacy agent-scoped skills + Library-attached skills, TASK-050). Each row shows a Live/Out-of-sync badge; AGENT-source rows get a Promote-to-Library action + delete; a 'Sync skills' header button pushes stale Library-attached skills to Meta and shows per-item results inline."
    },
    {
      "path": "frontend/src/components/agent-detail/SkillEditorModal.tsx",
      "purpose": "Add/edit skill form — title (client-side regex matching Meta's lowercase-hyphen format, server-side enforcement is TASK-043), description, instruction body (monospace, 20k char counter). librarySkillId prop (TASK-050): when set, saves to PUT /skills/{id} (Library row, never touches Meta) instead of the legacy agent-scoped endpoint; shows an inline notice that this edit won't reach Meta until the agent is synced. Reused (TASK-051) by SkillLibraryPage with agentId=\"\" — library-mode saves invalidate both ['skills-view', agentId] (no-op when reused from the Library page) and ['library-skills'] (partial key, matches every wabaId variant)."
    },
    {
      "path": "frontend/src/components/agent-detail/RunToolModal.tsx",
      "purpose": "Test-executes a connector tool (POST .../tools/{id}/run) — JSON input textarea with client-side parse validation, status+output display. Synchronous call, no polling (not a fire-and-forget job)"
    },
    {
      "path": "backend/src/main/resources/db/migration/V18__decouple_agent_waba_ownership.sql",
      "purpose": "agent.updated_by column + waba_account_access table + backfill grant from existing Waba.accountId (renamed from V12 to V18 to resolve a version collision with an already-existing V12 migration)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/entity/WabaAccountAccess.java",
      "purpose": "Account<->WABA access grant join table (mirrors client_staff) — 2026-07-28 decoupling: any granted account can see/edit every Agent on that WABA, not just the original creator"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/repository/WabaAccountAccessRepository.java",
      "purpose": "Repo for WabaAccountAccess — existsByWabaIdAndAccountId (access check), countByWabaId (shared-count indicator), findAllByAccountId (WabaService.list)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/waba/service/WabaAgentReconciliationService.java",
      "purpose": "Runs on WabaService.getPhones() view — grants the viewing account access, imports any phone number with a live Meta agent config that has no local Agent row yet. Best-effort per phone number, never fails the view"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/agent/service/AgentAccessService.java",
      "purpose": "Single place answering 'can this account touch this Agent' post-decoupling — every caller (AgentService, AgentDeployService, ReportsService, AnalyticsService, EvalRollupWorker) goes through here instead of AgentRepository accountId queries. Also populates Agent.sharedAccountCount (transient) for the frontend 'Shared WABA' indicator"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/persona/entity/BusinessProfile.java",
      "purpose": "Persona/business-profile entity mapped to Meta agent_config/business_info — DRAFT/DEPLOYED/ARCHIVED, accountId nullable for unmanaged rows discovered live on Meta"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/persona/repository/BusinessProfileRepository.java",
      "purpose": "BusinessProfile repo — account-scoped draft finders, per-phone-number DEPLOYED/ARCHIVED lookups"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/persona/service/BusinessProfileDeployService.java",
      "purpose": "Deploy sequence for BusinessProfile: GET Meta live business_info -> archive prior DEPLOYED row (or record drift) -> PUT new draft -> flip to DEPLOYED. Rolls back local archive if PUT fails; per-phoneNumberId lock guards against concurrent deploys. resetLive() (2026-07-28) resets Meta's business_info to defaults + archives the DEPLOYED row, same lock. getLive() (2026-08-03 Wave 1b) now backfills from Meta's real business_info GET when no local row exists (same pattern as Skills/FAQs/Files/Websites); backfill reuses deployLocks + a re-check inside the lock to prevent a duplicate-DEPLOYED-row race on concurrent first reads; backfilled rows stamp accountId to the requesting account; putBusinessInfo rejects an entirely-empty draft"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/persona/service/BusinessProfileDeployServiceTest.java",
      "purpose": "Covers 2026-08-03 Wave 1b backfill fix — including duplicate-row-prevention under concurrent getLive() and empty-payload rejection on putBusinessInfo"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/persona/controller/BusinessProfileController.java",
      "purpose": "REST surface for BusinessProfile drafts/live/history/deploy under /api/v1/business-profiles — DELETE /live (2026-07-28) resets Meta's business_info to defaults; delete on live/history ROWS in our own DB intentionally still not exposed pending founder's shared-delete-authorization decision (distinct from the reset endpoint)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/persona/dto/BusinessProfileDtos.java",
      "purpose": "SaveRequest/DeployRequest/BusinessProfileResponse DTOs for the BusinessProfile API. TASK-060: BusinessProfileResponse gained updatedAt (entity already had the column, just wasn't exposed) — needed for the Persona table's 'Last touched' column on drafts, which have no deployedAt/archivedAt yet."
    },
    {
      "path": "backend/src/main/resources/db/migration/V16__create_business_profile.sql",
      "purpose": "business_profile table — DRAFT/DEPLOYED/ARCHIVED status, phone_number_id, business_info fields, nullable account_id for unmanaged rows"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/client/entity/Client.java",
      "purpose": "Client entity (spec 2026-07-22-ia-revision.md module 5) — real table, account-scoped, references waba.id (not a duplicate), credit_line_status enum, createdBy/updatedBy audit columns"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/client/entity/ClientStaff.java",
      "purpose": "Staff<->client many-to-many join entity — a client can be shared between multiple internal staff, not owned by one owner_id"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/client/entity/ClientAuditLog.java",
      "purpose": "Append-only audit trail for Client edits — no Envers/JPA-auditing convention existed in this codebase, follows plain entity-table pattern"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/client/repository/ClientRepository.java",
      "purpose": "Client repo — findByIdAndAccountId (tenant fail-closed), findAllByAccountIdAndStaffUserId (staff-scoped list)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/client/repository/ClientStaffRepository.java",
      "purpose": "Client staff-grant repo — existsByClientIdAndUserId, findAllByClientId, deleteByClientIdAndUserId"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/client/repository/ClientAuditLogRepository.java",
      "purpose": "Client audit log repo — findAllByClientIdOrderByChangedAtDesc"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/client/service/ClientService.java",
      "purpose": "Client CRUD + staff access grants — verifyStaffAccess() fail-closed pattern (NotFoundException, mirrors AnalyticsService.verifyAgentOwnership); phone numbers reused live from WabaService.getPhones (no duplicate phone table); blocks removing last staff grant"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/client/controller/ClientController.java",
      "purpose": "REST /api/v1/clients — CRUD, staff access grant/revoke, phones (live from Meta via client's waba), audit-log read"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/client/dto/ClientDtos.java",
      "purpose": "Client DTOs — CreateRequest/UpdateRequest/StaffAccessRequest, ClientResponse (id as String), ClientDetailResponse, AuditEntryResponse"
    },
    {
      "path": "backend/src/main/resources/db/migration/V11__create_client_and_staff_access.sql",
      "purpose": "Migration: client table (FK to waba), client_staff join table (FK to users), client_audit_log table"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/client/service/ClientServiceTest.java",
      "purpose": "Integration tests ClientService — create/staff-grant, tenant+staff isolation fail-closed, audit log entries, last-staff-removal guard"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/webhook/controller/WebhookController.java",
      "purpose": "REST controller for /api/v1/webhook (single URL per Meta App, no accountId in path) — GET (hub challenge), POST (inbound payload). Verifies X-Hub-Signature-256 via HMAC-SHA256 + MessageDigest.isEqual (constant-time); skipped with WARN if meta.webhook.app-secret empty. Tenant resolved from payload phone_number_id -> AgentRepository.findByPhoneNumberId -> account; unattributable payloads dropped with 200. Persists to webhook_raw, publishes RabbitMQ event."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/webhook/entity/WebhookRaw.java",
      "purpose": "Raw webhook payload entity — stored before parsing, subject to retention job"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/webhook/listener/WebhookListener.java",
      "purpose": "RabbitMQ listener — consumes webhook.raw queue, delegates to ConversationService. accountId passed from WebhookRaw entity."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/webhook/repository/WebhookRawRepository.java",
      "purpose": "WebhookRaw JPA repository — claimForProcessing (atomic CAS, PENDING→PROCESSING), deleteProcessedBefore (retention cleanup)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/webhook/service/WebhookRetentionJob.java",
      "purpose": "Scheduled daily 02:00 purge of PROCESSED/FAILED webhook_raw rows older than webhook.retention.days (default 30)"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/infrastructure/claude/ClaudeApiClient.java",
      "purpose": "Claude API client - generate(systemPrompt,userPrompt), wizard-time only, 5s connect/10s read timeouts, fails fast to fallback"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/infrastructure/meta/MetaApiClient.java",
      "purpose": "Single point of contact for Meta APIs — RestClient (not WebClient). get/post/put/delete. meta.api.version property required. Every call logged via ApiCallLogWriter (async, best-effort, redacted) for the Reports > API Calls view — error response bodies now captured too (read inside onStatus before throwing, 2026-07-28 fix). scopedPath(basePath, metaAgentId) static helper appends ?agent_id= once one is recorded — required by settings/skills/connectors+tools, NOT by faq/website/files (confirmed via live testing)."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/infrastructure/meta/ThreadControlClient.java",
      "purpose": "Dedicated client for Meta Thread Control API (release action) — deliberately separate from MetaApiClient: different base URL shape, version 1.0.0 not 2.0.0, Bearer header + access_token/oauth_token query params. 5s/10s timeouts matching ClaudeApiClient pattern."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/infrastructure/meta/MetaApiException.java",
      "purpose": "Meta API error with HTTP status — isNotFound()/isAccessDenied() for spec 5.4 error mapping; responseBody field (2026-07-28) carries Meta's actual error body through to the api_call_log debug trail, best-effort, never used for control flow"
    },
    {
      "path": "backend/src/main/resources/application.yml",
      "purpose": "App config - port 8080, MySQL, Redis DB0/DB1, RabbitMQ, Meta API, Claude, JWT key paths, ratelimit.* limits (register/login/refresh/generate-defaults)"
    },
    {
      "path": "backend/src/main/resources/db/migration/V1__init.sql",
      "purpose": "Flyway V1 — initial schema: business_account, agent, agent_skill, agent_faq, agent_file, agent_website, user tables"
    },
    {
      "path": "backend/src/main/resources/db/migration/V2__production_schema.sql",
      "purpose": "Flyway V2 — production schema additions: conversations, messages, webhook_raw, analytics tables"
    },
    {
      "path": "backend/src/main/resources/db/migration/V3__add_system_prompt_to_agents.sql",
      "purpose": "Flyway V3 — adds system_prompt column to agent table"
    },
    {
      "path": "backend/src/main/resources/db/migration/V4__add_account_id_to_agent_child_entities.sql",
      "purpose": "Flyway V4 — adds account_id to agent_faq, agent_skill, agent_file, agent_website, agent_website_page for tenant isolation"
    },
    {
      "path": "backend/src/main/resources/db/migration/V5__normalize_ids_to_bigint_unsigned.sql",
      "purpose": "Flyway V5 — converts all PK/FK columns to BIGINT UNSIGNED to match TSID generator output"
    },
    {
      "path": "backend/src/main/resources/db/migration/V6__add_status_and_deployed_at_to_agent.sql",
      "purpose": "Flyway V6 — adds status (draft/active/paused/deleted) and deployed_at to agent table"
    },
    {
      "path": "backend/src/main/resources/db/migration/V7__messaging_stack_improvements.sql",
      "purpose": "Flyway V7 — adds interactive to messages.content_type enum; adds idx_conversations_account_last_message composite index"
    },
    {
      "path": "backend/src/main/resources/db/migration/V8__create_waba_and_nullable_agent_phone.sql",
      "purpose": "Migration: waba table (TSID, account-scoped, unique account_id+waba_id); agent.phone_number_id nullable + agent.waba_id column"
    },
    {
      "path": "backend/src/main/resources/db/migration/V9__agent_wizard_fields.sql",
      "purpose": "Migration - additive nullable wizard fields: customer_facing_name, channel enum, tone, language, behavior_rules"
    },
    {
      "path": "backend/src/main/resources/db/migration/V10__add_handoff_fields_to_agent.sql",
      "purpose": "Migration - additive handoff_enabled (NOT NULL DEFAULT FALSE) + nullable handoff_message on agent table"
    },
    {
      "path": "backend/src/main/resources/db/migration/V10__seed_demo_waba.sql",
      "purpose": "Migration - seeds demo account WABA row for out-of-the-box bind-phone flow"
    },
    {
      "path": "backend/src/main/resources/db/migration/V11__add_needs_human_to_conversation.sql",
      "purpose": "Migration - adds conversations.needs_human boolean (default false) — scaffold for standby/handoff detection"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/common/id/TsidGeneratorTest.java",
      "purpose": "Unit test — TsidGenerator: uniqueness, time-ordering, NODE_ID encoding"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/templatestudio/TemplateStudioServiceTest.java",
      "purpose": "Real MySQL via Testcontainers, real SecretEncryptor round-trip, only TemplateStudioClient mocked (network boundary). Rewritten 2026-08-04 for the corrected per-esme_addr credential model. Covers: tenant isolation (no WABA access -> NotFoundException), zero-mapping -> BusinessException, decrypted key + real Karix waba_id passed to client, esme_addr reuse across 2 phone numbers, duplicate-phone-mapping rejection, duplicate-esme_addr rejection, mapping rejected when account has no WABA access, never exposes the raw key via any DTO. Actually executed (not just statically reviewed) — exit 0, all 8 tests pass against real Testcontainers MySQL."
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/infrastructure/crypto/SecretEncryptorTest.java",
      "purpose": "Pure unit test, no Spring context — round-trip, distinct ciphertext per call (random IV), wrong key fails, tampered ciphertext fails (GCM auth tag), rejects non-32-byte key. Actually executed 2026-08-04, passed."
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/agent/service/AgentDeployServiceTest.java",
      "purpose": "Integration test — AgentDeployService: deploy sets active, pause sets paused, Meta API first"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/agent/service/AgentServiceTest.java",
      "purpose": "Integration test — AgentService: createAgent, addFaq, addSkill, tenant isolation"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/conversation/service/ConversationServiceTest.java",
      "purpose": "Integration test — full webhook→parse→store→analytics flow via RabbitMQ"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/conversation/service/ConversationStoreTest.java",
      "purpose": "Integration test — ConversationStore: findOrCreate idempotency, saveMessage"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/conversation/service/InboundMessageParserTest.java",
      "purpose": "Unit tests — InboundMessageParser: valid message, missing fields, non-text types"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/conversation/service/MetaMessageSenderTest.java",
      "purpose": "Unit tests — MetaMessageSender: success returns ID, missing ID throws BusinessException"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/user/controller/AuthControllerTest.java",
      "purpose": "Integration test — AuthController: register, login (200+cookies), wrong password (400), lockout (400 after 5 fails)"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/support/IntegrationTestBase.java",
      "purpose": "Test base — MySQLContainer + RedisContainer (Testcontainers). @MockBean MetaApiClient + ClaudeApiClient. NODE_ID=0."
    },
    {
      "path": "backend/src/test/resources/application-test.yml",
      "purpose": "Test profile — BCrypt cost 4, Flyway enabled, RSA test key pair inline"
    },
    {
      "path": "boris-SKILL.md",
      "purpose": "Boris skill notes — proactive engineering workflow guidance"
    },
    {
      "path": "docs/audit_index.py",
      "purpose": "Drift audit script — diffs knowledge-index.json against files on disk; run after any batch of changes"
    },
    {
      "path": "docs/infrastructure.md",
      "purpose": "AWS server details — SSH bastion (13.232.241.246), app server (10.1.17.16), installed services, ports, credentials (dev only). karix-mcp (port 8000/8001) is no longer just \"leave running, unrelated\" (2026-08-03) — this platform's Template Studio module proxies to its new REST API, so a karix-mcp deploy/outage can now break a feature here that it couldn't before."
    },
    {
      "path": "karix-mcp/",
      "purpose": "Separate Python/FastMCP service (own README, own tests, own deploy — NOT part of this platform's Java build). Source imported into this monorepo 2026-08-03. Exposes WhatsApp send/template tools over MCP (Convogent, Claude Desktop) plus a REST API (create/list/get/edit/delete template, media upload, bulk-import, audit-log) that domain/templatestudio (Java) proxies to. karix_client.py already had list_templates/get_template/edit_template/upload_media implemented before 2026-08-04 — that day's work was wiring rest.py + template_ops.py + app.py routes to expose them, not building new Karix integration. GET /api/audit-log (added 2026-08-04) reads the api_call_log table that karix_client.py's _retry() already writes to on every call — no new logging, just a new read path, path_prefix-filterable so a WABA's audit view can't see unrelated activity. See karix-mcp/README.md for architecture, endpoints, known Karix API quirks (e.g. the image-media-handle bug) — do not duplicate that detail here."
    },
    {
      "path": "docs/knowledge-index.json",
      "purpose": "This file — flat codebase index, updated on every approved change"
    },
    {
      "path": "docs/meta-api/INDEX.md",
      "purpose": "Master index of all Meta Business Agent APIs — paths, methods, setup sequence"
    },
    {
      "path": "docs/meta-api/agent-eval.md",
      "purpose": "GET/POST /agent-eval — test and evaluate agent performance"
    },
    {
      "path": "docs/meta-api/agent-event.md",
      "purpose": "POST/GET /agent_event — trigger agent from business events"
    },
    {
      "path": "docs/meta-api/agent-onboarding.md",
      "purpose": "POST /agent_onboarding — trigger AI agent onboarding for entity+channel, schedules async data-prep jobs. Added 2026-08-04. NOT implemented anywhere in this codebase — AgentService.createAgent() currently goes straight to PUT agent_config/settings without calling this first. Flagged for EM review, not silently wired in."
    },
    {
      "path": "docs/meta-api/business_info.md",
      "purpose": "GET/PUT/DELETE /agent_config/business_info — payment/return/purchase/delivery/contact info. Backfilled 2026-08-04 from founder-pasted official spec; implementation (BusinessProfileDeployService) already existed and was confirmed live before this doc did."
    },
    {
      "path": "docs/meta-api/ui-skills.md",
      "purpose": "GET/POST/PUT/DELETE /agent-ui-skills — rich-message components (carousel/CTA/flow/image/list/location), separate surface from plain text-instruction Skills. Added 2026-08-04, genuinely new capability, NOT implemented anywhere in this codebase yet."
    },
    {
      "path": "docs/meta-api/delete-agent.md",
      "purpose": "DELETE /delete_agent — remove agent from phone number. Added 2026-08-04, formalizes an already-implemented endpoint (AgentDeployService.deleteFromMeta())."
    },
    {
      "path": "docs/meta-api/agent-test.md",
      "purpose": "POST /agent_test — send test messages, get agent response, multi-turn support"
    },
    {
      "path": "docs/meta-api/allowlist.md",
      "purpose": "CRUD /agent_config/allowlist — restrict agent to specific phone numbers"
    },
    {
      "path": "docs/meta-api/connector-tools.md",
      "purpose": "CRUD + run /agent_connectors/{id}/tools — individual operations per connector"
    },
    {
      "path": "docs/meta-api/connectors.md",
      "purpose": "CRUD /agent_connectors — external API integrations, auth upserts, logs"
    },
    {
      "path": "docs/meta-api/eligibility.md",
      "purpose": "GET /agent_eligibility — check if phone number can use Meta Business Agent"
    },
    {
      "path": "docs/meta-api/faq.md",
      "purpose": "CRUD /agent_config/faq — Q&A knowledge base"
    },
    {
      "path": "docs/meta-api/files.md",
      "purpose": "GET/POST/DELETE /agent_config/files — document uploads (PDF, DOCX, images, CSV, XLSX)"
    },
    {
      "path": "docs/meta-api/settings.md",
      "purpose": "GET/PUT /agent_config/settings — enable/disable agent (rollout.enabled), handoff, followup, audience"
    },
    {
      "path": "docs/meta-api/skills.md",
      "purpose": "CRUD /agent_config/skills — agent behavior instructions (tone, triggers, flows)"
    },
    {
      "path": "docs/meta-api/thread-control.md",
      "purpose": "POST /thread_control — hand conversation back to Meta Business Agent (v1.0.0 API, different base URL); corrected note points to webhook-standby-handoff.md for the real handoff signal"
    },
    {
      "path": "docs/meta-api/webhook-standby-handoff.md",
      "purpose": "Real observed webhook shapes from partner pilot payloads (Jul 2026) — handoff signal is standby-wrapper presence/absence, NOT a messaging_handovers field; parsing rule for webhook controller"
    },
    {
      "path": "docs/meta-api/websites.md",
      "purpose": "CRUD /agent_config/websites — URLs for Meta to crawl natively"
    },
    {
      "path": "docs/specs/2026-07-18-architecture.md",
      "purpose": "System architecture — component diagram, data flows, full schema, API surface, security, async topology, caching, deployment, risks"
    },
    {
      "path": "docs/specs/2026-07-18-product-design.md",
      "purpose": "PM spec — all UX/product decisions: nav structure, wizard flow, WABA onboarding, dashboard, reports, P0/P1 screens"
    },
    {
      "path": "docs/specs/2026-07-22-world-class-audit.md",
      "purpose": "Full PM+UX+Design Evaluator+EM audit — reframes users as internal operators (not consumer SMB owners); ranked findings: no client-switcher, no test-before-deploy, inbox has no triage, webhook secret silently optional, DESIGN.md mood mismatch"
    },
    {
      "path": "docs/specs/2026-07-22-ia-revision.md",
      "purpose": "Supersedes nav IA in 2026-07-18-product-design.md — 5-module structure (Dashboard/Agents/Connectors-Skills-KB-FAQ library/Chat Conversation/Profile-Customer); flags module-3 WABA-level shared library and module-4 takeover-vs-readonly as needing EM/founder decisions before build"
    },
    {
      "path": "frontend/.oxlintrc.json",
      "purpose": "Oxlint config for frontend"
    },
    {
      "path": "frontend/README.md",
      "purpose": "Frontend readme — Vite + React + TS setup notes"
    },
    {
      "path": "frontend/components.json",
      "purpose": "Shadcn config — default style, CSS variables, @ alias, component output to src/components/ui/"
    },
    {
      "path": "frontend/index.html",
      "purpose": "Vite HTML entry — Inter font import lives here (DESIGN.md token location)"
    },
    {
      "path": "frontend/package.json",
      "purpose": "Frontend deps — React 18, TypeScript 5, Vite, Tailwind 3, Shadcn/ui, TanStack Query, Zustand, React Router, Axios, React Hook Form, Zod"
    },
    {
      "path": "frontend/postcss.config.js",
      "purpose": "PostCSS config — Tailwind + autoprefixer (required by Tailwind CSS)"
    },
    {
      "path": "frontend/src/App.css",
      "purpose": "Legacy Vite scaffold CSS — mostly unused; global styles live in index.css"
    },
    {
      "path": "frontend/src/App.tsx",
      "purpose": "Root — QueryClientProvider, BrowserRouter. \"/\" now renders RootRedirect (2026-08-04, Netflix-style module picker): reads cached entitlements (no extra fetch), redirects straight to the module's home route if exactly ONE is enabled (zero friction for today's 100% single-module reality), else to /select (ModuleSelectorPage). /select sits under ProtectedRoute but OUTSIDE AppShell (account-level chrome, not feature nav) — all deep-links (/agents/123, sessionStorage last-agent:{tab} resume) bypass this entirely, only \"/\" itself runs the redirect decision. Unmatched routes now redirect to \"/\" (not directly to /dashboard) so the entitlement-aware logic still applies. Protected routes via ProtectedRoute+AppShell: /dashboard, /templates + /templates/iris + /templates/settings (Template Studio's nav, split 2026-08-04, Campaigns tab dropped same day in favor of a Send action on Templates + Iris chat), /agents, /agents/new, /agents/:id, /library/skills, /wabas, /inbox, /handover, /profile."
    },
    {
      "path": "frontend/src/pages/TemplateStudioPage.tsx",
      "purpose": "Templates section of Template Studio. Dashboard redesign 2026-08-04 (PM+EM+UX+EL+Design-Evaluator, EL rejected once for duplicate GET /templates/{wabaId} fetching, UX rejected once for missing pagination + no counter loading state — both fixed and re-approved) after founder rejected the prior full-page 'configure credentials' block screen. WabaTemplateStudio now ALWAYS renders the dashboard shell — an unconfigured WABA shows zero counts everywhere plus a small dashed-border inline banner, never a wall. TemplateListPanel: 4 analytics counter cards (Total/Approved/Pending/Rejected, skeleton-loading via countersLoading, not a misleading '0') computed from allQuery (base unfiltered fetch, also serves the table when no filter is picked) with a separate filteredQuery only firing once a status is chosen (avoids the double-fetch EL caught) — table itself is a real `<table>` (name/category/language/quality/status/actions) with client-side pagination (PAGE_SIZE=10, Previous/Next, resets to page 0 on filter change). classifyStatus() is the single source of truth for status classification, shared by both the counter tally and the status pill (Design Evaluator caught these using two different matching rules pre-fix — regex pill vs exact-match counter — which could silently disagree on e.g. 'Submitted'). Template name / rejection-reason cells truncate with title tooltip to prevent row-height blowout. TemplateBuilderForm/HeaderEditor/BodyEditor/FooterEditor/ButtonsEditor/TemplateMetaFields unchanged from prior pass — edit mode still seeds via seedFromComponents() before allowing submit (Karix's edit API replaces components entirely). Design Evaluator PASS notes (advisory, not actioned): counter-card row is the most-cloned Tailwind admin-dashboard pattern (stock-Shadcn-dashboard feel), worth a future pass to differentiate (e.g. segmented total bar instead of 4 twin cards, or drop decorative icon chips). Iris (TemplateIrisPage.tsx) calls the SAME /api/v1/templates/** endpoints via TemplateStudioService, never a separate implementation."
    },
    {
      "path": "frontend/src/pages/TemplateSettingsPage.tsx",
      "purpose": "Settings section of Template Studio (2026-08-04) — always reachable (rotate a credential, connect a phone) unlike the old inline blocking screen it replaced. ConnectedPhonesPanel (rewritten 2026-08-04 for the corrected credential model) lists phone->esme_addr mappings for the WABA; ConnectPhoneForm has two modes — reuse an existing esme_addr (account-wide dropdown, prevents accidentally creating duplicate esme_addr rows with the same api_key) or add a brand-new one — defaults to \"new\" mode when the account has zero existing esme_addr options (EL round-1 non-blocking note, fixed same day). Phone-number dropdown sources only unmapped phones. Also has AuditLogPanel (added 2026-08-04) — auto-loads on mount (EL round-1 caught an enabled:false that made it never auto-load, only fixed to load-on-mount + manual Refresh button), expandable rows showing method/path/status/request+response body for every karix-mcp call under /api/templates for this WABA. Bodies are pre-redacted server-side (api_call_logger.py) before they're ever stored — safe to render raw."
    },
    {
      "path": "frontend/src/pages/TemplateIrisPage.tsx",
      "purpose": "Iris section of Template Studio. SECOND redesign 2026-08-04 (PM+EM+UX+EL x3+Design-Evaluator x2 — one real BLOCK, not a rubber stamp) — founder explicitly wanted Iris to feel like Claude.ai's own UI (calm, text-first, generous whitespace), after being shown a fake scripted HTML mockup and liking its structural ideas (hero state, live split-pane preview) but NOT its invented color palette or its dropped-in-2026-07-20 Campaigns nav concept. Superseded the FIRST redesign's avatar-chip treatment (EM-confirmed acceptable reversal: founder's later, more specific instruction overrides the prior UX recommendation). Current shape: (1) hero/empty state — headline + pill input + 3 plain underlined-text suggestions (NOT icon chips — Design Evaluator's first pass BLOCKed a gradient-sparkle avatar icon + icon-per-chip pattern as generic-AI-wrapper styling, e.g. every Copilot/GPT-wrapper SaaS; removed entirely across two fix rounds, including the credential-setup card's icon chip and the connected-state pill badge, which is now a single plain text line 'Using {provider}/{model} · Change'); (2) IrisChat messages: Iris's own replies render as plain prose (NO avatar, NO bubble/border) while the user's messages keep a navy filled bubble, right-aligned — the asymmetry itself is the 'text-first' signal; (3) PendingActionPreview: split-pane layout (grid-template-columns 1fr 360px, desktop-only by design, documented in a code comment — no responsive breakpoint since this is an internal operator tool) replaces the old JSON-dump confirm panel with a REAL WhatsApp-style bubble render (fieldsForCreateOrEdit() reads pendingToolArgs.components — same Meta component shape TemplateStudioPage.tsx's seedFromComponents() consumes) for create_template/edit_template specifically; other tool types (send_test_template) still fall back to raw JSON. WhatsApp chrome uses the sanctioned whatsapp-header/canvas/bubble/ink Tailwind tokens (EL caught raw hex bypassing these on first pass, fixed). AiCredentialSetup: BYOK provider/model closed-set dropdowns from GET /templates/iris/credential/options (CLAUDE + NVIDIA_LLAMA, OPENAI deferred P1). IrisChat: session-per-WABA, POST /templates/iris/sessions/{id}/messages, needsConfirmation/pendingToolName/pendingToolArgs contract unchanged — zero backend changes across either redesign round. No frontend/DESIGN.md exists yet — flagged three times now (both UX gates + both Design Evaluator gates) as worth authoring before the next chat-surface screen."
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/domain/templatestudio/iris/",
      "purpose": "Iris backend (2026-08-04) — locked scope: create/edit/list templates, send TEST templates, marketing conversation. Fixed tool allowlist enforced in IrisConversationService, never trusted to system-prompt instruction alone. Key files: AiProvider (closed-set provider+model enum — CLAUDE, plus NVIDIA_LLAMA added same day as the 2nd real opt-in BYOK provider (meta/llama-3.3-70b-instruct); never add a provider here without a matching adapter), AiProviderCredential/AiCredentialService (BYOK, account-scoped — one ACTIVE provider per account at a time, upsert() deletes any other-provider row when switching; EL-caught bug same day: getStatus()/resolveForConversation() hardcoded AiProvider.CLAUDE.name(), so a non-Claude credential was silently invisible — fixed to pick firstCredentialForAccount() via a new findAllByAccountId repo method, not assume which provider), AiMessage/AiToolSpec/AiTurnResult/AiProviderAdapter (neutral tool-calling representation — one small adapter per provider, tool implementations never change per provider; IrisConversationService needs ZERO changes to add a provider, confirmed true for NVIDIA_LLAMA), ClaudeAdapter (calls Anthropic's Messages API directly with `tools` — deliberately NOT built on ClaudeApiClient, which is wizard-only/platform-key/no-tool-support per its own contract), NvidiaLlamaAdapter (OpenAI-compatible chat completions; tool_calls[].function.arguments is a JSON STRING here, unlike Claude's already-object `input` — parsed via objectMapper.readValue, wrapped in BusinessException on malformed JSON; has a package-private test-only constructor accepting a pre-built RestClient so MockRestServiceServer tests don't get clobbered by the real constructor's own requestFactory-setting logic — the real constructor needs @Autowired since 2+ constructors exist, EL-caught the SAME \"stray @RequiredArgsConstructor fighting a manual constructor\" bug class here that hit KarixMessagingClient), IrisSession/IrisMessage (chat persistence; session's pending_tool_name/pending_tool_args_json hold a mutating tool call's exact args awaiting confirmation — confirm() always replays these persisted args, never a fresh model message, which is what \"confirm-before-submit\" actually means at the data layer), IrisConversationService (the loop: list_templates executes inline since it's read-only; create_template/edit_template/send_test_template pause-persist-return-preview instead of executing, per the locked confirm-before-submit requirement), KarixMessagingClient (send_test_template's in-house direct-to-Karix client — founder's explicit call to bypass karix-mcp for this, since Karix's send API only needs the plain api_key as a Bearer header, no OAuth exchange, and karix-mcp has no REST endpoint for sending anyway; reuses TemplateStudioService.resolveCredential, now public for this reuse). No-WABA-picker change (2026-08-04, PM+EM+UX+EL approved): a session no longer binds to one wabaId upfront (CreateSessionRequest.wabaId was already nullable, IrisSession.wabaId column already nullable — no migration needed). executeTool() ALWAYS read wabaId from the model's own tool-call arguments, never from session.getWabaId(), which was pure unused metadata — so nothing needed to change there. What changed: SYSTEM_PROMPT_BASE is now a %s-templated string, formatted per-turn in sendMessage() with describeWabas(wabaService.listForAccount(session.getAccountId())) — Iris is told every WABA the account can see (label + wabaId) and is instructed to ask which one if 2+, use the only one silently if 1, and always name the resolved WABA back to the operator before proposing any action (PM-mandated safety net against silent misresolution). Tenant isolation: executeTool() now takes the already-fetched List<Waba> accountWabas (passed from sendMessage/confirmPendingAction, no duplicate listForAccount() call) and hard-rejects any model-supplied wabaId not in that list BEFORE dispatching to TemplateStudioService/KarixMessagingClient — this is on top of, not instead of, those services' own existing wabaAccountAccessRepository checks; it's the one chokepoint ALL Iris tool dispatch passes through. Frontend (TemplateIrisPage.tsx): WabaPicker/useSelectedWaba removed entirely; UX gate caught two real gaps on first pass — a missing zero-WABA guard (NoWabaState component, dashed border + Link to /wabas, takes priority over both credential-setup and chat) and no expectation-setting for the ask-which-WABA behavior (one-line hint shown only when wabaCount > 1). Chat-history sidebar + cross-page sync (2026-08-04, PM+EM+EL x2+UX+Design-Evaluator approved) — V36 migration adds iris_session.title (varchar 120, nullable, set once from a truncated first-user-message via truncateForTitle(), never reset). New IrisSessionSummaryProjection (id/title/updatedAt only — sidebar list never hydrates the pending_tool_args_json TEXT column). New conversationHistory() private helper is the SINGLE shared USER/ASSISTANT-filtered read path for both sendMessage()'s model-history-building and the new getMessages() endpoint — deliberately factored out so the two can't drift on what counts as displayable conversation vs internal tool bookkeeping (EM's explicit requirement). New endpoints: GET /sessions (listSessions, accountId-scoped via SecurityContextHelper, same tenant pattern as everywhere else), GET /sessions/{id}/messages (getMessages, behind requireOwnedSession). Known v1 gap (EL+UX reviewed, judged acceptable — not silently broken): resuming a session that had a pending confirmation shows no visual indicator of that state; the backend's existing pendingToolName guard in sendMessage() independently prevents any double-execution regardless, so the worst case is a confusing-but-safe error on next send, not a security/correctness bug. Frontend: TemplateIrisPage.tsx's IrisChat became IrisWorkspace (220px SessionSidebar + chat, fixed width, no responsive breakpoint — same desktop-only reasoning as the split-pane preview). Design Evaluator caught one real generic-AI-clone tell on first pass (a MessageSquare icon on every session row — the one element that would look identical in any AI chat clone) and one improvement (add a small uppercase 'Recent' label matching PendingActionPreview's existing typographic pattern) — both fixed. New shared frontend/src/lib/templateQueryKeys.ts (templateQueryKeys.list(wabaId)) used by BOTH TemplateStudioPage.tsx (its query keys) and TemplateIrisPage.tsx (invalidates after Iris confirms create_template/edit_template, using the resolved wabaId from pending.args.wabaId, since Iris no longer knows a fixed wabaId client-side) — prevents the two pages' independently hand-typed ['templates', wabaId, ...] arrays from silently drifting apart. Self-caught TSID precision bug (2026-08-04, same day, EL re-reviewed): SessionSummary.id and TurnResponse.sessionId were Long, serializing TSIDs (18-digit values, well past JS Number.MAX_SAFE_INTEGER) as raw JSON numbers instead of strings — same class of bug the project already solved once for WabaResponse (see WabaDtos.java's 'TSID as string' comment). Fixed: IrisSession.id got @JsonSerialize(using=ToStringSerializer.class) (fixes createSession's raw-entity response too), SessionSummary/TurnResponse fields changed to String, all construction sites use String.valueOf(...). Caught by re-checking against the established WabaResponse convention after noticing a real 18-digit WABA id earlier in the same session — not caught by the first EL pass, a reminder that 'compiles clean' isn't the same as 'no data-corruption bug.'"
    },
    {
      "path": "frontend/src/hooks/useSelectedWaba.ts",
      "purpose": "Shared WABA-list + selected-WABA state (2026-08-04) for Template Studio's 3 nav sections — localStorage-persisted (key template-studio-waba) so picking a WABA on one section keeps it selected when switching to another. validSelectedId clears the selection if the persisted id isn't in the fetched WABA list; all current consumers gate on isLoading before reading it, so the momentary empty-during-fetch value is never surfaced (EL note 2026-08-04: a future consumer skipping that gate could see a false reset — worth a comment if this hook grows more callers)."
    },
    {
      "path": "frontend/src/components/templatestudio/WabaPicker.tsx",
      "purpose": "Shared WABA-select dropdown (2026-08-04), used by all Template Studio sections via useSelectedWaba — extracted so the picker UI can't drift between Iris/Templates/Settings."
    },
    {
      "path": "frontend/src/pages/ModuleSelectorPage.tsx",
      "purpose": "Netflix-profile-style feature picker (2026-08-04) — one card per Module (Business Agents, Template Studio), greyed + \"Not enabled for this account\" when disabled per useModuleEntitlements. Only reached when 2+ modules enabled (see App.tsx RootRedirect). Not wrapped in AppShell."
    },
    {
      "path": "frontend/src/components/layout/AppShell.tsx",
      "purpose": "App shell — collapsible brand-navy sidebar (localStorage sidebar-collapsed), off-canvas mobile drawer (dialog ARIA, Escape, backdrop, matchMedia reset), topbar breadcrumb + toggles with aria-controls. Nav is module-aware (2026-08-04 fix — was hardcoded to Business Agents even on /templates): activeNav = TEMPLATE_STUDIO_NAV when path starts with /templates, else NAV. Business Agents nav: Dashboard, Agents (grouped sub-nav: Knowledge Base/Connectors — deep-link to sessionStorage last-agent:{tab} or /agents?pickFor={tab} picker mode; Skills — static route to /library/skills, TASK-051; Business Persona — static route to /library/persona, TASK-058 — both route to a standalone Library page instead of a deep-link, since both have account-wide drafts not yet attached to any agent; hidden in collapsed rail), WABAs, Inbox, Human Handover (soon), Profile. Template Studio nav (2026-08-04, Campaigns tab dropped same day — see TemplateIrisPage.tsx): Iris, Templates (end:true, so it doesn't stay highlighted on its sibling routes), Settings — all literal /templates/* siblings, not nested pages of Templates. Iris reuses the MessageSquare icon also used by Business Agents' Inbox item — safe since the two nav arrays are never rendered together. Breadcrumb resolves exact-path match first, then prefix match, then activeNav[0] as fallback (two-pass, order-independent — fixed 2026-08-04 from a single combined condition that was order-dependent)."
    },
    {
      "path": "frontend/src/components/router/ProtectedRoute.tsx",
      "purpose": "Auth guard — reads isAuthenticated from Zustand, renders Outlet or Navigate to /login. Module-gated (2026-08-03): also fails closed (locked screen, not Outlet) when useModuleEntitlements is loading→ok, but errors OR literally zero modules enabled (2026-08-04: changed from hardcoded BUSINESS_AGENTS check to Object.values(...).some(Boolean) — a Template-Studio-only account must not be wrongly locked out). No frontend test infra exists yet to cover this path (flagged as a follow-up, not silently dropped)."
    },
    {
      "path": "frontend/src/components/waba/ConnectPhoneModal.tsx",
      "purpose": "Two-step WABA connect modal — validate WABA ID, pick phone (bound ones disabled), persist WABA + bind to agent"
    },
    {
      "path": "frontend/src/hooks/useAuth.ts",
      "purpose": "Auth hooks — useLogin, useRegister (mutate→setUser→/dashboard), useLogout (mutate→clearAuth→/login), useMe (query /auth/me, staleTime 5min)"
    },
    {
      "path": "frontend/src/hooks/useModuleEntitlements.ts",
      "purpose": "Queries GET /api/v1/modules/entitlements (2026-08-03) — single source of truth ProtectedRoute/RootRedirect/ModuleSelectorPage all share; only enabled once authenticated. ModuleName type: 'BUSINESS_AGENTS' | 'TEMPLATE_STUDIO' (2026-08-04)"
    },
    {
      "path": "frontend/src/index.css",
      "purpose": "Global styles — Tailwind directives, Shadcn CSS variable definitions, Karix primary=navy accent=pink (dark: 327 60% 45%), Inter font import"
    },
    {
      "path": "frontend/src/lib/api.ts",
      "purpose": "Axios instance — baseURL from VITE_API_URL, withCredentials:true for httpOnly cookie, 401 interceptor redirects to /login"
    },
    {
      "path": "frontend/src/lib/utils.ts",
      "purpose": "cn() utility — merges Tailwind classes via clsx + tailwind-merge, used by all Shadcn components"
    },
    {
      "path": "frontend/src/main.tsx",
      "purpose": "React entry point — mounts App into #root, wraps with QueryClient and Router"
    },
    {
      "path": "frontend/src/pages/AgentDetailPage.tsx",
      "purpose": "Agent detail — overview (deploy/pause, Connect phone number modal, Thread Control release button+confirm modal shown when handoffEnabled), Knowledge Base/Skills/Connectors/Business Profile/Eval/Settings tabs; initial tab seeded from ?tab= query param, writes sessionStorage last-agent:{tab}=id on every tab view (feeds AppShell's grouped Agents sub-nav deep-links); Delete-from-Meta danger block, Trigger Event action; 'Shared WABA' badge in header when sharedAccountCount>1; maps ApiResponse envelope + entity fields, string TSIDs. TASK-062: WebsitesSection/FilesSection were previously entirely unwired placeholders (hardcoded '0', TASK-036 TODO comments) — now real list/add/upload/delete wired to the backend, with a shared NotSyncedBadge component (also used by FaqsSection, refactored from inline duplicate markup) showing metaSynced=false. File upload MUST pass `headers: { 'Content-Type': undefined }` on the multipart POST — the shared api instance's default application/json header would otherwise override FormData's automatic boundary detection (EL REJECT caught this, and a hardcoded 'multipart/form-data' string is equally wrong — it strips the boundary= param). AudienceSection component (2026-08-04, in Settings tab) — ai_audience toggle + allowlist number CRUD; one list serves both QA-test-numbers and phased-rollout use cases (PM: same mechanism, not two features); guards against enabling ALLOWLISTED_ONLY with zero numbers (would silently block everyone with no visible explanation)."
    },
    {
      "path": "frontend/src/pages/AgentsPage.tsx",
      "purpose": "Agents list table — name/phone/WABA ID/conversation count (GET /conversations/counts)/health (derived: status+phone, not Meta quality rating)/status/last updated; 'Shared' badge next to name when sharedAccountCount>1. Picker mode (?pickFor=knowledge|skills|connectors, from AppShell's Agents sub-nav when no agent viewed yet this session): swaps header to 'Choose an agent to view its {X}', row click routes to /agents/{id}?tab={pickFor}. React Query, loading/empty states."
    },
    {
      "path": "frontend/src/pages/CreateAgentPage.tsx",
      "purpose": "5-step Create Agent wizard - Identity/Personality(AI generate)/Knowledge FAQs/Connections placeholder/Go live, 3-panel layout with live WhatsApp preview, autosave draft on step 1"
    },
    {
      "path": "frontend/src/pages/DashboardPage.tsx",
      "purpose": "Dashboard — attention-triage landing page. Client-side heuristic over GET /agents (no backend attention signal yet): active+no-phone > paused > draft, sorted by urgency. Empty/all-clear/error states, loading skeleton. React Query. TASK-061: PhoneNumbersTable gained a QualityBadge (GREEN/YELLOW/RED — unconfirmed against any doc, defensive fallback for unknown values) showing Meta's quality_rating per number, the leading indicator before WhatsApp restricts/bans it."
    },
    {
      "path": "frontend/src/pages/InboxPage.tsx",
      "purpose": "Conversation Audit Log — 2-panel read-only viewer. Left: conversation list (GET /conversations?accountId=). Right: message thread (GET /conversations/:id/messages). conversationId in URL param. accountId from auth store."
    },
    {
      "path": "frontend/src/pages/LoginPage.tsx",
      "purpose": "Login/Register page — split panel (navy brand left, white form right). Tab toggle. RHF+Zod validation. brand-pink CTA."
    },
    {
      "path": "frontend/src/pages/SettingsPage.tsx",
      "purpose": "Account Settings — display name, email (read-only badge), plan (Starter hardcoded for P0), Danger Zone with disabled delete button + support contact."
    },
    {
      "path": "frontend/src/store/authStore.ts",
      "purpose": "Zustand persist store — user, isAuthenticated, setUser, clearAuth. Persisted to localStorage key 'auth'."
    },
    {
      "path": "frontend/tailwind.config.js",
      "purpose": "Tailwind config — Karix brand tokens, whatsapp.* mimicry tokens for chat preview, shadcn semantic CSS-variable tokens, Inter, radius scale"
    },
    {
      "path": "frontend/tsconfig.app.json",
      "purpose": "TypeScript app config — strict flags, bundler moduleResolution, @ path alias, ignoreDeprecations 6.0 for baseUrl"
    },
    {
      "path": "frontend/tsconfig.json",
      "purpose": "TypeScript project references — points to tsconfig.app.json and tsconfig.node.json"
    },
    {
      "path": "frontend/tsconfig.node.json",
      "purpose": "TypeScript config for Vite config file (node environment)"
    },
    {
      "path": "frontend/vite.config.ts",
      "purpose": "Vite config — React plugin, @ path alias pointing to src/"
    },
    {
      "path": "wiki/INDEX.md",
      "purpose": "Obsidian wiki root index — project knowledge vault entry point"
    },
    {
      "path": "wiki/api/INDEX.md",
      "purpose": "Wiki API section index"
    },
    {
      "path": "wiki/architecture/INDEX.md",
      "purpose": "Wiki architecture section index"
    },
    {
      "path": "wiki/architecture/messaging-stack.md",
      "purpose": "Wiki — messaging stack architecture (webhooks, idempotency, retention)"
    },
    {
      "path": "wiki/architecture/schema.md",
      "purpose": "Wiki — database schema notes"
    },
    {
      "path": "wiki/architecture/system-overview.md",
      "purpose": "Wiki — system overview (Meta runs AI natively, we are the config layer)"
    },
    {
      "path": "wiki/bugs-violations/INDEX.md",
      "purpose": "Wiki bugs/violations section index"
    },
    {
      "path": "wiki/bugs-violations/violations-2026-07-20.md",
      "purpose": "Wiki — process violations log 2026-07-20"
    },
    {
      "path": "wiki/decisions/INDEX.md",
      "purpose": "Wiki decisions section index"
    },
    {
      "path": "wiki/decisions/maker-checker.md",
      "purpose": "Wiki — maker-checker gate decision record"
    },
    {
      "path": "wiki/decisions/stack-frontend.md",
      "purpose": "Wiki — frontend stack decision (Shadcn/Tailwind over Ant Design)"
    },
    {
      "path": "wiki/deployment/INDEX.md",
      "purpose": "Wiki deployment section index"
    },
    {
      "path": "wiki/deployment/lessons.md",
      "purpose": "Wiki — deployment lessons (9 hard-won AWS deploy lessons)"
    },
    {
      "path": "wiki/deployment/runbook.md",
      "purpose": "Wiki — production deploy runbook (bastion SSH path, systemd, /var/www/metaagent)"
    },
    {
      "path": "wiki/docs/INDEX.md",
      "purpose": "Wiki docs section index"
    },
    {
      "path": "wiki/frontend/INDEX.md",
      "purpose": "Wiki frontend section index"
    },
    {
      "path": "wiki/frontend/brand.md",
      "purpose": "Wiki — Karix brand notes (superseded by DESIGN.md for authority)"
    },
    {
      "path": "wiki/sessions/INDEX.md",
      "purpose": "Wiki sessions section index"
    },
    {
      "path": "wiki/sessions/session-2026-07-20.md",
      "purpose": "Wiki — session log 2026-07-20"
    },
    {
      "path": "backend/src/main/java/com/metaagent/platform/common/security/RateLimitFilter.java",
      "purpose": "Fixed-window rate limiting filter (Redis DB0 INCR+EXPIRE). Covers register/login/refresh/generate-defaults. Fails open if Redis down. 429 + Retry-After + ApiResponse envelope. Refresh keyed by hashed refresh_token cookie"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/common/security/RateLimitFilterTest.java",
      "purpose": "Unit test RateLimitFilter - passthrough, under/over limit 429, fail-open on Redis error, refresh token-hash key, first-increment expiry"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/analytics/service/AnalyticsServiceTest.java",
      "purpose": "Integration test AnalyticsService - owner summary, cross-tenant NotFound (incl empty-data bypass regression), hourly cross-tenant, empty summary for owner"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/waba/service/WabaServiceTest.java",
      "purpose": "Integration test WabaService - validate happy path, phone connected flag, persistence with caller accountId, error paths"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/conversation/service/StatusUpdateParserTest.java",
      "purpose": "Unit test StatusUpdateParser - sent/delivered/read/failed, missing statuses, malformed JSON -> empty"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/webhook/controller/WebhookControllerSignatureTest.java",
      "purpose": "Integration test webhook X-Hub-Signature-256 (valid 200, invalid/missing 403), hub challenge GET, and payload tenant resolution (phone_number_id -> agent account persisted; unknown -> dropped with 200)"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/webhook/service/WebhookRetentionJobTest.java",
      "purpose": "Integration test WebhookRetentionJob - purges old PROCESSED/FAILED, keeps PENDING and recent rows"
    },
    {
      "path": "backend/src/test/java/com/metaagent/platform/domain/conversation/service/HandoffClassifierTest.java",
      "purpose": "Pure unit tests HandoffClassifier - standby-wrapped contacts/messages, standby message_echoes, top-level messages no standby/statuses (NEEDS_HUMAN), statuses array, empty/malformed payload"
    }
  ]
}